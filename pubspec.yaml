name: epub_reader
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  epubx: ^3.0.0
  flutter:
    sdk: flutter
  logger: ^1.1.0
  path_provider: ^2.0.11
  webview_flutter: ^3.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  assets:
    - packages/epub_reader/js/slide.js
    - packages/epub_reader/js/styles.css
